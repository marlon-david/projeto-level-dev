<% if notice.present? %>
  <p style="color: green"><%= notice %></p>
<% end %>

<div>
  <% @topics.each do |t| %>
    <%= render t %>
    <p><%= link_to "show", t %></p>
    <hr />
  <% end %>
</div>

<%= link_to "novo tópico", new_topic_path %>